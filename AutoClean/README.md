python3 AutoCleaner.py              # run cleanup
python3 AutoCleaner.py --dry-run    # preview only

Requires python3. Some tasks need root (apt, journal, snap, docker).
Optional: notify-send for desktop notification.
